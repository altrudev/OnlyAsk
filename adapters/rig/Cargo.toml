[package]
name = "onlyask-rig"
version = "0.3.0"
edition = "2024"
license = "Apache-2.0"
description = "Rig runtime adapter and conformance gate for OnlyAsk"

[dependencies]
rig = "=0.42.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
